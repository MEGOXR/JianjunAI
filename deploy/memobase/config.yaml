# Memobase Configuration (Flat Structure)

server_host: "0.0.0.0"
server_port: 8019
log_level: "info"

# LLM Config
# 使用 openai provider + Azure OpenAI v1 兼容端点 (与 embedding 相同)
llm_style: "openai"
llm_api_key: "placeholder_will_be_overridden"
llm_base_url: "placeholder_will_be_overridden"
best_llm_model: "gpt-4o-mini"

# Embedding Config
# 使用 openai provider + Azure OpenAI v1 兼容端点
embedding_provider: "openai"
# 以下值通过环境变量覆盖 (MEMOBASE_EMBEDDING_*)
embedding_api_key: "placeholder_will_be_overridden"
embedding_base_url: "placeholder_will_be_overridden"
embedding_model: "text-embedding-3-large"
embedding_dim: 3072

# Auth Config
auth_enabled: true
# 您的专属 API Key
api_key: "mb_sk_73928174928374928374982374982374"
